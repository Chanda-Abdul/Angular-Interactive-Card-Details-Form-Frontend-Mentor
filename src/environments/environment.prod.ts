export const environment = {
  production: true
  /* add api keys here */
   // firebaseAPIKey: 'black-blah-blah' (as + enviroment.firebaseAPIkey)

};


/*
- [ ] After we add
auth.service.tx
import { environement } from '../../this.file' */
